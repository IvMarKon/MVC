{
  "name": "ppch-sorts",
  "version": "1.0.0",
  "description": "just js sorts",
  "scripts": {
    "test": "gulp lint",
    "serve": "npm install && gulp watch",
    "debug": "gulp debug",
    "precommit": "npm test",
    "prepush": "npm test"
  },
  "author": "Ivan Markov",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.21.0",
    "console-browserify": "^1.1.0",
    "gulp": "^3.9.1",
    "gulp-eslint": "^4.0.0"
  }
}
