{
  "name": "ppch-sorts",
  "version": "1.0.0",
  "description": "just js sorts",
  "scripts": {
    "test": "gulp lint",
    "serve_sorts": "npm install && gulp watch-sorts",
    "debug_sorts": "gulp debug-sorts",
    "serve_patterns": "npm install && gulp watch-patterns",
    "debug_patterns": "gulp debug-patterns",
    "precommit": "npm test",
    "prepush": "npm test"
  },
  "author": "Ivan Markov",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.21.0",
    "console-browserify": "^1.1.0",
    "gulp": "^3.9.1",
    "gulp-eslint": "^4.0.0"
  }
}
